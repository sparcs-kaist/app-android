# Global Robolectric config for JVM unit tests.
# Use a plain Application (not the @HiltAndroidApp one) so tests don't boot the Hilt
# graph / TokenStorage (AndroidKeyStore isn't available on the JVM). ViewModels are
# constructed manually in tests.
sdk=34
application=android.app.Application
